name: Remote-only Release

on:
  push:
    tags:
      - "remote-v*"

permissions:
  contents: write

jobs:
  build-and-publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install dependencies
        run: npm ci

      - name: Build remote-only Tauri app
        uses: tauri-apps/tauri-action@v0.5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          VITE_NEXQ_REMOTE_ONLY: "true"
        with:
          args: --features remote-only -- --no-default-features
          tagName: ${{ github.ref_name }}
          releaseName: "NexQ Remote-only ${{ github.ref_name }}"
          releaseBody: "Remote-only build: cloud STT/translation/LLM, local audio capture and SQLite history."
          releaseDraft: false
          prerelease: true
          includeUpdaterJson: true
