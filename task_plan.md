# Task Plan: Qwen 纯 API 版四项功能调整

## Goal

在不引入本地 AI provider、也不修改耳机/输入设备逻辑的前提下，完成问答与翻译链路修复、回答语言选择、悬浮窗透明度调节、知识库批量删除与文件夹切换，并通过构建和功能回归验证。

## Current Phase

Phase 5 — Delivery

## Scope and Acceptance Summary

### 1. 问答触发与翻译逻辑

- 检查并修复语音分段、最终段判定、上下文拼接、模式判断和回答触发链路。
- 确认提问不会只触发翻译而漏掉回答；不应依赖空格等偶然输入才能回答。
- 校准自动翻译的来源文本、目标语言、说话方/角色和上下文，避免错配。
- 增加可重复的单元/集成回归场景，覆盖自动翻译、提问回答和连续语音段。

### 2. 回答语言可选

- 设置中提供三种模式：中文、English、中英双语。
- 选择持久化，重启后保留上次选择。
- 回答提示词和生成链路统一读取该设置。
- 双语输出固定为“先中文、后英文”，避免中英文交错混排。
- 默认值为中英双语（中文在前），仍保留中文和 English 单语选项。

### 3. 悬浮窗透明度

- 在设置中增加滑块控制透明度。
- 置顶悬浮窗运行时可即时生效，不要求重启或重新打开窗口。
- 持久化用户设置，重启后恢复。
- 明确最小/最大值、步进、默认值，并验证 Windows/Tauri 窗口行为。

### 4. 知识库文件夹管理

- 支持多选/批量删除知识库文件。
- 支持对整个已管理知识库文件夹执行一次确认后删除。
- 支持选择/切换新的知识库目录。
- 支持清空旧知识库索引和文件后重新导入新目录。
- 删除操作必须同步清理 SQLite 索引、应用记录和实际由应用管理的文件；不得误删目录外文件。

## Explicitly Out of Scope

- 耳机、麦克风、输入设备和用户自己的输入设置问题已经排除，不修改相关软件逻辑。
- 不恢复或扩展本地 Whisper、Ollama、LM Studio、OPUS-MT 等 provider。
- 不把本项目改造成无 UI 的独立 HTTP API 服务；本计划中的“纯 API”沿用仓库定义的 Qwen 云端 API 桌面版本。

## Phases

### Phase 1: Requirements & Discovery

- [x] 固化四项需求和验收标准
- [x] 记录耳机/输入问题为范围外
- [x] 确认当前分支已经是 Qwen 纯 API 默认构建
- [x] 初步定位问答、翻译、设置持久化和知识库 IPC 的实际代码入口
- [x] 完成窗口控制入口和可测试 seam 定位
- [x] 为问答/翻译问题列出可证伪的候选原因
- **Status:** complete

### Phase 2: Planning & Technical Design

- [x] 画出语音段 → 翻译 → 问题识别 → 回答的现有调用链
- [x] 设计回答语言设置的数据结构、默认值、迁移策略和提示词注入位置
- [x] 设计透明度设置的窗口同步与持久化方案
- [x] 设计知识库文件夹/文件的安全删除、索引清理和目录切换流程
- [x] 为每项功能定义回归测试矩阵
- **Status:** complete

### Phase 3: Implementation

- [x] 修复问答触发和自动翻译逻辑
- [x] 增加中文 / English / 中英双语回答模式
- [x] 增加悬浮窗设置和实时同步
- [x] 完善知识库批量删除、文件夹删除、目录切换和重导入
- [x] 保持纯 API provider 边界和现有数据兼容
- **Status:** complete

### Phase 4: Testing & Verification

- [x] TypeScript/Vite 构建
- [x] Rust `remote-only` 构建检查
- [x] 问答触发、连续分段、翻译角色/上下文回归
- [x] 三种回答语言和重启持久化回归
- [x] 透明度滑块、实时生效和重启恢复回归
- [x] 知识库删除、索引清理、目录切换和重新导入回归
- [x] 检查工作树 diff，确认没有误改输入设备范围
- **Status:** complete

### Phase 5: Delivery

- [x] 更新本计划、发现和进度记录
- [x] 汇总改动文件、测试结果和已知限制
- [x] 交付可运行的纯 API 版本调整
- **Status:** complete

## Key Questions

1. 当前回答触发是由最终语音段、问题检测器、快捷键/空格事件，还是多个条件共同决定？
2. 自动翻译应以哪一方的原文为源，目标语言如何由会议模式和用户设置决定？
3. 知识库“文件夹”是通过导入路径推导，还是已有持久化 folder/entity 模型？
4. Tauri 当前窗口创建和权限配置是否支持跨平台/Windows 的运行时透明度调整？
5. 删除知识库文件夹时，哪些路径由应用创建并可安全删除，哪些只能移除索引？

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| 只维护 Qwen 纯 API 路径 | 用户明确只需要纯 API 版本；当前仓库已将其设为默认构建 |
| 默认回答语言使用中英双语 | 直接满足用户的双语显示要求，同时保留单语选项供用户切换 |
| 双语格式固定为中文在前、英文在后 | 满足用户要求并减少流式输出中的混排 |
| 所有新设置沿用现有持久化配置体系 | 便于记忆上次选择，并减少新增存储机制和迁移风险 |
| 知识库删除采用确认 + 索引/文件分层清理 | 需求包含删除实际文件，必须控制误删范围并保证索引一致性 |
| 自动回答由 launcher 统一调度，后端所有最终 Them 段统一发 `question_detected` | 不依赖 overlay 是否及时挂载，修复纯 API 音频事件丢失 |
| 知识库以导入来源目录作为 folder group，删除只删除 app context 副本 | 支持文件夹管理，同时保护用户原始目录 |
| 原生透明度 API 不在当前 Tauri JS API 中，保留 CSS 背景 alpha 并同步 overlay DOM | 当前 Windows/Tauri 依赖可稳定验证，避免调用不存在的 API |

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| 基线检查中的 `rg` 正则括号未闭合 | 1 | 改用 PowerShell `Select-String` 完成定位；未影响仓库 |
| 首次 `cargo check` 后续检查短暂等待 build directory 文件锁 | 1 | 等待前一个编译完成后再次检查，最终退出码为 0 |

## Notes

- 每完成一个阶段，更新本文件的状态，并同步更新 `findings.md` 与 `progress.md`。
- 重大代码决策前先重新阅读本文件。
- 所有错误和测试结果都要记录，避免重复失败。
